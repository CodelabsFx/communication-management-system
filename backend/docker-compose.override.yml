version: '3.9'

services:
  backend:
    volumes:
      - ./backend:/src
    command: ["/bin/sh", "-c", "go run ./cmd/api"]

  frontend:
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
